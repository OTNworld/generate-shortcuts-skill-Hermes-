# MCP server (FastMCP)

```bash
# one-time
/opt/homebrew/bin/python3 -m venv .venv-mcp
.venv-mcp/bin/pip install -r mcp_server/requirements.txt

# run (Cursor uses this via mcp.json)
./mcp_server/run.sh
```

Doc: [`../references/MCP_SHORTCUTS.md`](../references/MCP_SHORTCUTS.md)
