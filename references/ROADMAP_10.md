# Roadmap: 10/10 (next)

Palier A (9/10) is implemented in v1.6.0. Remaining for a credible **10/10 skill**:

| ID | Item | Status |
|----|------|--------|
| B1 | Make `check_shortcut_grammar.py --strict` the default CI gate + range-vs-string-length checks | Scaffolded; deepen |
| B2 | Starter palette 12–20 full XML schemas (`templates/palette/`) | Todo |
| B3 | Fill `fixtures/attested/MATRIX.md` with real macOS/iOS import proofs | Scaffolded |
| B4 | Generate markdown catalogs from `data/*.json` (`scripts/render_refs.py`) | Todo |
| B5 | `SECURITY.md` | Done (prep) |
| B6 | `SKILL.en.md` + `OUTPUT_NAMES.md` | Todo |
| B7 | Share Sheet / ImportQuestions golden (`09-share-sheet-input`) | Todo |
| B8 | `URL_SCHEMES.md` + Locally attested golden or design-only md | **URL_SCHEMES done**; Locally attest still todo |
| B9 | `scripts/extract_shortcut.sh` (`plutil` round-trip helper) | Todo |
| B10 | Expand community vendors from `data/external/*.index.jsonl` as needed | **Hub started** (`ECOSYSTEM.md`, 2 goldens) |

Ecosystem hub: `references/ECOSYSTEM.md` + `data/sources.json`.

See `CONTRIBUTING.md` and `fixtures/attested/README.md`.
