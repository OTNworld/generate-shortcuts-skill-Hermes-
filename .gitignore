# macOS system junk
.DS_Store
.AppleDouble
.LSOverride
.localized

# Signed outputs / generated shortcuts (XML goldens use *.shortcut.xml)
*_signed.shortcut
*.shortcut
!*.shortcut.xml

# Local attestation scratch
/out/
/fixtures/attested/*.shortcut
/fixtures/attested/*_signed.shortcut
fixtures/attested/runs/

__pycache__/
*.py[cod]

# Horizon app fetch output (seed / local CI smoke)
/horizon/app/Vendor/
