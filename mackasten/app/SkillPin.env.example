# Copy to SkillPin.env in the private app repo (do not commit secrets).
# SKILL_REF must be a tag or SHA for release builds — not main.

SKILL_REPO_URL=https://github.com/OTNworld/generate-shortcuts-skill-Hermes-.git
SKILL_REF=main
